#include <iostream>;
using namespace std;
main()
{
	int a;
	cout<<"Enter the age of the user""\n";
	cin>>a;
	if(a>=18)
	cout<<"User is eligible to vote";
	else
	cout<<"User is not eligible to vote";
}
